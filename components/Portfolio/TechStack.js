const TechStack = ({ project }) => {
  return (
    <ul className="flex justify-center flex-wrap gap-1 list-none p-0">
      {/* {project.stack.map((item) => (
        <li
          className="inline-block grow border-[1px] border-neutral-700 text-gray-300 px-4 rounded text-xs font-semibold font-mono text-center"
          key={item}>
          {item}
        </li>
      ))} */}
    </ul>
  )
}

export default TechStack
